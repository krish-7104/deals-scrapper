/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["m.media-amazon.com", "rukminim2.flixcart.com", "assets.myntassets.com", "assets.ajio.com", "images.meesho.com"]
    }
};

export default nextConfig;
